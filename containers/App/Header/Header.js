import React from 'react';
import {StyledHeader, HeaderLogo} from './Header.styled'

function Header(){
  return (
  <  StyledHeader>
    <HeaderLogo>CASSIOPEIA</HeaderLogo>
  </StyledHeader>
  );
}

export default Header;